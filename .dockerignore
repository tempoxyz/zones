**/.git
# Foundry's lockfile check requires Git metadata. Docker uses the already
# checked-out submodule sources; contract CI validates their locked revisions.
crates/contracts/foundry.lock
contrib/
docs/
localnet/
scripts/*
!scripts/run-prover-host.sh
target/
