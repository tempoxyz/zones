name: Reproducible Build

# Builds and hashes the byte-deterministic `tempo-zone` binary via the shared
# reusable workflow in tempoxyz/gh-actions.

permissions: {}

concurrency:
  group: reproducible-build-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

on:
  push:
    branches: [main]

  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref (branch, tag, or full SHA) to build reproducibly"
        type: string
        required: false
        default: "main"

jobs:
  build:
    uses: tempoxyz/gh-actions/.github/workflows/reproducible-build.yml@b35994a23aaf6acf4d5e2f91d18235a6e2f90e95 # main
    permissions:
      contents: read
      id-token: write
    with:
      binary-name: tempo-zone
      ref: ${{ inputs.ref }}
